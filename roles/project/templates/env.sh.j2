# {{ ansible_managed }}

export APP_ENV={{app_env}}
export RAILS_ENV={{app_env}}
export RAKE_ENV={{app_env}}

{% if extra_env %}
{% for k in extra_env %}
export {{k}}="{{extra_env[k]}}"
{% endfor %}
{% endif %}
