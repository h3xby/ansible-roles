# {{ ansible_managed }}

export APP_ENV={{tier}}
export RAILS_ENV={{tier}}
export RAKE_ENV={{tier}}

{% if extra_env %}
{% for k in extra_env %}
export {{k}}="{{extra_env[k]}}"
{% endfor %}
{% endif %}
